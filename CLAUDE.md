# Simple Editor — read first

Before doing anything in this repo, read [agents.md](agents.md) (workflow, conventions,
verification), [goals.md](goals.md) (priorities, budgets, UX principles, what's already done),
and [notes.md](notes.md) (running scratchpad). This applies to every task, not just the ones
that use the `/se-*` slash commands defined in `.claude/commands/`.
